; This is to autoboot the program on start-up, on the Pi. CREATE THE SERVICE FILE:

; sudo nano /etc/systemd/system/argus.service

[Unit]
Description=Argus Facial Recognition System
After=multi-user.target sound.target
Wants=sound.target

[Service]
Type=simple
User=pi
WorkingDirectory=/home/pi/argus
ExecStart=/usr/bin/python3 /home/pi/argus/main.py
Restart=on-failure
RestartSec=5
# Gives the audio subsystem time to initialise after boot before the script starts
ExecStartPre=/bin/sleep 5
Environment=PYTHONUNBUFFERED=1
# Required for audio when running as a systemd service (not a login session)
Environment=XDG_RUNTIME_DIR=/run/user/1000
Environment=PULSE_RUNTIME_PATH=/run/user/1000/pulse

[Install]
WantedBy=multi-user.target
